# PostgreSQL Configuration
POSTGRES_USER=trello
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=trello

# Backend Configuration
DATABASE_URL="postgresql://trello:change_this_password@postgres:5432/trello?schema=public"
PORT=3001
NODE_ENV=production

# Frontend Port (for production)
# PORT=80
